# How It Works

tscope reads from one of two local data sources (selected per run via `--source`):

## Data Sources

### OTel (default)

When [OTel is configured](installation.md), tscope reads from the shared export file:

```
~/.copilot/tscope/otel.jsonl
```

This file is written by the Copilot CLI's OpenTelemetry exporter in real time — one NDJSON record per event. tscope processes only **`span` records whose `name` starts with `chat `** (e.g. `"chat claude-opus-4.7"`). Other record types are ignored:

- **`invoke_agent` spans** — excluded (they wrap `chat` spans and would double-count tokens)
- **`metric` records** — excluded (they are aggregate histograms with no per-session scope)
- **`execute_tool` spans** — excluded

Sessions are identified by the `gen_ai.conversation.id` attribute, which is the same UUID as the session-state directory name. tscope groups `chat` spans by this ID to build per-session token and cost totals.

**OTel coverage is forward-only.** Sessions that started before `tscope otel enable --apply` was run have no OTel data; use `--source logs` to see them.

### Log parser (historical / fallback)

When OTel is not configured (or `--source logs` is explicit), tscope scans:

```
~/.copilot/session-state/<session-id>/events.jsonl
```

Each session directory contains an `events.jsonl` with event records including a `session.shutdown` event that holds per-model token metrics.

## Source Selection — No Merging

**One source is used per run; sources are never mixed.** In `auto` mode (default), tscope checks whether `~/.copilot/tscope/otel.jsonl` exists and is non-empty. If so, OTel is used for the entire run; otherwise the log parser is used and a fallback notice is printed to stderr.

Use `--source otel` or `--source logs` to override explicitly.

## OTel Token Extraction

For each `chat` span, tscope reads these OTel attributes:

| OTel attribute | Token field |
|---|---|
| `gen_ai.usage.input_tokens` | Input tokens (includes cache subsets) |
| `gen_ai.usage.output_tokens` | Output tokens |
| `gen_ai.usage.cache_read_input_tokens` | Cache read tokens |
| `gen_ai.usage.cache_creation_input_tokens` | Cache write tokens |
| `gen_ai.usage.reasoning_output_tokens` | Reasoning tokens |
| `github.copilot.nano_aiu` | Server-side billing (÷ 1e9 = AI credits) |

The model name comes from `gen_ai.response.model` (preferred) or `gen_ai.request.model`. Spans with no `gen_ai.conversation.id` are skipped. Malformed lines are silently ignored.

## Log Parser Token Extraction

Each session's `events.jsonl` contains a `session.shutdown` event with per-model token metrics:

- **Input tokens** — the grand total of tokens sent to the model; **includes** the cache read and cache write tokens below
- **Output tokens** — tokens generated by the model
- **Cache read tokens** — cached prompt tokens, avoid re-processing (a *subset* of input)
- **Cache write tokens** — tokens written to cache (a *subset* of input)

Because cache read/write are already part of input, the non-double-counted session total is **`input + output`** (reported as **Total (I/O)**).

> A `reasoningTokens` field is also parsed from the session events and surfaced in the JSON output for completeness, but it is **not** displayed in the text or HTML reports for log-parser sessions.

## Resumed Sessions

A **resumed** session contains one `session.shutdown` per run and each run's metrics reset to zero, so `tscope` **sums** the per-model usage across every shutdown event to produce true cumulative totals. This applies to the log parser only; OTel spans accumulate naturally.

## Sessions With No Token Data

`tscope` silently excludes any session that has no billable token activity. This includes:

- **In-progress sessions** — those without a `session.shutdown` event (no token data has been recorded yet).
- **Completed sessions with empty or all-zero token metrics** — those whose `session.shutdown` event recorded an empty `modelMetrics` map, or models whose input and output counts are all zero.

The exclusion applies uniformly to all three output formats: text, JSON, and HTML.

For JSON output, `summary.inProgressCount` is always `0` (retained for schema shape) and `summary.sessionCount` equals `summary.completedCount`.
